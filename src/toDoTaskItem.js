// priority can be low, medium or high add priorityValue for user filter method
 const taskItem = (title, desc, dueDate, priority, priorityVal, completed, id) => {

  return { id, title, desc, dueDate, priority, priorityVal, completed}
 };

  export default taskItem

